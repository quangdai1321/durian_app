/**
 * Centralized app configuration.
 * All URLs, API keys, feature flags go here.
 */

// Backend URL — chỉ cần đổi 1 chỗ khi deploy
export const API_BASE_URL = "https://durianapp-production.up.railway.app";
export const API_URL      = `${API_BASE_URL}/api`;
